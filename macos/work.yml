---
- hosts: localhost
  tasks:
    - name: install python packages
      ansible.builtin.pip:
        name:
          - azure-identity
          - azure-mgmt-compute
          - azure-mgmt-network
          - urlwatch
          - yapf
        executable: /usr/local/bin/pip3
