---
- name: Rebuild initramfs
  ansible.builtin.command: /usr/sbin/mkinitramfs -o /boot/initramfs.gz
  register: rebuilt_initramfs
  changed_when: rebuilt_initramfs.rc == 0
  failed_when: rebuilt_initramfs.rc != 0
  environment:
    CRYPTSETUP: y
