---
- name: install vpn client
  when: mtype == 'work'
  become: true
  pacman: name=networkmanager-fortisslvpn

- name: install required tools
  when: mtype == 'work'
  become: true
  pacman:
    name:
      - colordiff
      - direnv
      - git-crypt
      - asdf-vm
