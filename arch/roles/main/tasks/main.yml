---
- name: install ansible-extras
  become: true
  pacman:
    name:
      - ansible-lint
      - python-passlib
      - sshpass
      - yapf

- name: install downgrade
  kewlfft.aur.aur: name=downgrade

- include: apps.yml
- include: cli.yml
