---
tailscale_exit_node_hosts:
  - parkpi
  - raipi

tailscale_host_tags:
  atlas:
    - tag:service
  homepi:
    - tag:homeassistant
  iris:
    - tag:service
  kleio:
    - tag:service
  parkpi:
    - tag:vpn
  raipi:
    - tag:vpn
  talos:
    - tag:service
  tvpi:
    - tag:service

tailscale_ssh_hosts:
  - atlas
  - homepi
  - iris
  - kleio
  - parkpi
  - raipi
  - talos
  - tvpi
